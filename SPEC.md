# 邮件管家 Agent / Email Steward Agent

**SPEC_STATUS = APPROVED**  
**批准日期：2026-09-13**  
**产品版本：V1**

## 1. 身份、使命与使用者

邮件管家 Agent 是一个面向非技术营销人员的 Codex 项目工作区。它帮助操作者安全地读取个人阿里企业邮箱中的开发合作邮件，整理行动信息，起草中英文回复，并且只在操作者逐封明确确认后发送邮件。

- **主要使用者：** 使用个人阿里企业邮箱的营销团队成员。
- **运行环境：** 操作者自己的 Windows 或 macOS 电脑。
- **核心目标：** 将“查邮件、理解邮件、写回复”的日常工作变成清晰、可验证、低门槛的 Codex 工作流。
- **非目标：** 替代人工商务判断、自动代表操作者承诺价格或条款、自动对外发送邮件。

## 2. V1 范围

### 必须具备

1. GitHub 私有仓库与 ZIP 两种分发方式。
2. 操作者将安装包交给 Codex 并说“帮我安装这个 Agent”后，由 Agent 引导首次安装。
3. 安装前说明如何在阿里企业邮箱网页端生成第三方客户端安全密码：
   `设置 → 账户与安全 → 账户安全 → 三方客户端登录安全管理 → 生成新密码`。
   必须提醒操作者立刻复制并安全保存，因为密码只显示一次。
4. 安装时询问项目文件夹位置、操作者姓名、个人企业邮箱、首选语言、默认回复语言与语气。
5. 同时支持 Windows 和 macOS。
6. 使用阿里企业邮箱默认 SSL 服务：IMAP `imap.qiye.aliyun.com:993` 与 SMTP `smtp.qiye.aliyun.com:465`。
7. 密码仅可在本地隐藏输入窗口中输入；Windows 保存到 Credential Manager，macOS 保存到 Keychain。密码不得出现在聊天、代码、配置、日志、Git 或测试资料中。
8. 自动执行一次只读 IMAP 连接检查。
9. 查询、读取和总结邮件，不改变已读状态；按文件夹、日期、主题、发件人、收件人、抄送和会话检索。
10. 在 Codex 中起草中文或英文新邮件、回复和回复全部；草稿不保存到邮箱。
11. 支持 To、Cc、Bcc；发送前完整展示发件身份、To、Cc、Bcc、主题、正文及会话来源。
12. 逐封、当前对话内的明确确认才可发送。发送不确定时不得自动重试。
13. 可选星标：先列出精确文件夹、邮件 UID、主题和理由，再等待逐项确认。
14. 每日日报为可选功能，默认关闭。启用前必须确认日报维度、时间、时区、范围和格式。
15. 提供完整中英文安装、使用、升级与故障排查指南。
16. 使用真实邮箱完成首次验收：查询邮件并生成回复草稿；不强制发送真实邮件。

### 明确不做

- 不自动发送、批量发送或定时发送邮件。
- 不删除、移动、复制、重命名邮件或文件夹。
- 不改变已读/未读状态。
- 不创建邮箱草稿。
- 不发送附件或自动下载危险附件。
- 不使用浏览器、网页邮箱或 GUI 自动化作为邮箱访问后备方案。
- 不共享或集中保存不同操作者的邮箱凭据、邮件正文或个人状态数据。
- 不默认开启日报，不预建外部 IM、CRM、表格或通知系统集成。
- 不默认使用 Sol、高推理或更高推理等级。

## 3. 架构与运行方式

- **解决方案架构：** T5 工具集成型工作流 Agent。
- **复杂度：** L4。
- **Agent 运行时：** `CODEX_NATIVE`。
- **主交互渠道：** Codex。
- **交付策略：** `CODEX_FIRST`。
- **触发方式：** `MIXED: ON_DEMAND + optional SCHEDULED`。
- **定时基础设施：** 默认 `NONE`；操作者启用日报后，使用该 Codex 项目中的本地定时自动化。
- **外部集成：** 仅阿里企业邮箱 IMAP/SMTP。

### 渠道地图

| 渠道 | 角色 | 允许的动作 |
|---|---|---|
| Codex 项目对话 | `INTERACTIVE`、`APPROVAL`、`DELIVERY` | 查询、分析、草稿、逐封发送确认、星标确认、结果展示 |
| 阿里企业邮箱 IMAP | 外部集成 | 只读查询、读取安全附件元数据/内容、经确认星标 |
| 阿里企业邮箱 SMTP | 外部集成 | 仅发送已明确确认的一封邮件 |
| Codex 定时自动化（可选） | `INBOUND_TRIGGER` | 仅读取与分析新未读邮件，生成日报 |

## 4. 模型与推理策略

本 Agent 的协议访问、状态去重、权限检查和发送校验必须优先由确定性程序完成。模型仅负责语言理解、摘要、分类和写作。

| 用户任务 | 推荐模型 | 推理等级 | 原因 |
|---|---|---|---|
| 邮箱连接、查找、读取、分类、日报 | Luna | 低 | 任务结构固定，以速度和成本控制为先 |
| 撰写、改写、翻译、润色邮件 | Terra + humanizer | 低 | 需要自然、可信的语言表达，但不需要复杂任务规划 |
| 发送前最终检查 | Terra | 低 | 核对完整邮件内容和会话信息 |

- 不推荐 Sol、高推理或更高推理等级。
- 信息不足、条款不清、收件人不明确或存在商务风险时，Agent 必须请求操作者补充信息，而不是升级模型或猜测。
- 为了避免操作者反复切换模型，项目使用两个固定用途的对话：`邮件查询与日报（Luna，低推理）` 与 `邮件写作与发送（Terra，低推理）`。

## 5. 首次安装与初始化

### 5.1 安装入口

操作者可以：

1. 从 GitHub 私有仓库下载发布版本；或
2. 获得 ZIP 安装包并交给 Codex。

当操作者说“帮我安装这个 Agent”时，安装引导必须先识别包版本和操作系统，再分阶段执行。未获得确认前，不得创建项目文件夹或写入配置。

### 5.2 引导流程

1. 说明安装目的、预计时长、邮箱安全边界。
2. 展示第三方客户端安全密码的网页端生成路径，并要求操作者先准备密码。
3. 询问项目文件夹位置，展示完整目标路径，获得确认后创建工作区。
4. 检查所需运行环境；缺失依赖由安装程序自行安装或给出通俗、可恢复的说明。
5. 询问姓名、个人邮箱、语言与默认写作偏好。
6. 以本地隐藏输入窗口收集安全密码；绝不在聊天中索取或回显。
7. 用默认 SSL 参数执行只读 IMAP 测试。
8. 询问是否启用每日日报，默认“不启用”。选择启用时再采集日报配置。
9. 引导操作者在 Codex 创建一个以该工作区为范围的长期项目，名称建议为`邮件管家 - <姓名>`。
10. 引导操作者在新项目内完成首次验收。

### 5.3 首次验收

安装对话本身不执行验收。操作者必须进入新创建的 Codex 项目：

1. 新开对话，选择 **Luna + 低推理**，发送：

   `请完成首次验收：查询最近 7 天未读的开发合作邮件，展示一封最相关邮件的摘要和邮件编号。不要发送邮件。`

2. 再新开对话，选择 **Terra + 低推理**，发送：

   `请根据邮件编号【复制上一步的编号】，生成一份专业、友好的英文回复草稿。不要发送邮件。`

3. 只有查询和草稿均成功、且邮件保持未读，初始化状态才可以标记为成功。

## 6. 邮件工作流

### 6.1 查询与阅读

- 每次查询必须明确搜索范围：文件夹及已提供的日期、主题、From、To、Cc 条件。
- 默认搜寻 INBOX 及操作者配置的归档文件夹；仅在操作者明确要求完整往来时纳入 Sent。
- 每封邮件使用 `(folder, UIDVALIDITY, UID)` 标识；不得依赖会变化的序号或仅依赖主题。
- 使用只读方式取得邮件，严禁把邮件标为已读。
- 返回来源、收件人、日期、主题、会话标头、正文及附件清单。

### 6.2 附件

- 自动展示附件名称、类型和大小。
- 仅对 PDF、Office（非宏）、CSV/TXT 和图片等安全业务文件进行临时提取和分析。
- 不执行或解压宏、脚本、可执行文件、压缩包或宏启用文档。
- 原始附件仅置于临时目录，分析结束后删除；不进入持久状态、Git 或报告。

### 6.3 草稿与发送

- 草稿始终只显示在 Codex 对话中，明确标记“未保存、未发送”。
- 每份草稿在事实核对后，必须通过 `humanizer` 进行一次自然化润色，去除模板腔、夸张宣传语、空泛客套、机械句式和明显的 AI 写作痕迹。
- `humanizer` 只能调整表达，必须保留已确认的事实、姓名、公司、产品、承诺、金额、日期、截止时间、收件人、抄送、密送和邮件语气；信息不足时必须保留占位符或询问操作者，不能为了自然化而补造细节。
- 操作者提供过往邮件范例时，`humanizer` 应优先贴近该操作者的真实写作风格；未提供范例时，使用简洁、自然、专业的默认商务风格。
- 草稿展示时应标明“已完成自然化润色”，但不应声称可以保证邮件无法被任何工具识别为 AI 写作。
- 回复必须基于已读取的原邮件，保留 `Message-ID` 与 `References`；缺失会话标头时阻止发送。
- 发送前必须展示：发送身份、To、Cc、Bcc、主题、正文、是否新邮件/回复/回复全部、关联邮件编号。
- 只有操作者在当前对话明确确认该唯一内容时才可调用 SMTP。
- 发送结果只能报告：已发送、确定未发送、结果不确定。结果不确定时停止并要求人工决定后续动作。

### 6.4 星标

- Agent 可以提出星标建议，但不得自动执行。
- 每个建议必须展示来源文件夹、UID、主题和理由。
- 仅对当前对话中逐项确认的邮件应用 IMAP `\\Flagged`。
- 不得创建自定义标签、移动邮件或重试不确定结果。

### 6.5 可选每日日报

- 默认关闭。
- 启用后仅处理上次成功运行到当前运行之间的新未读 INBOX 邮件。
- 使用本地、最小化的身份与语义卡片去重；保留期最多 90 天，不保留原始正文、附件或草稿。
- 运行失败时不推进水位线、不自动重试、不改变邮件状态。
- 启用前必须配置日报标题、分类维度、行动清单格式、运行时间与时区。

## 7. 可复用 Skills

1. `business-email-management`：唯一的凭据、IMAP 与 SMTP 边界。
2. `mail-imap-read`：只读查找、读取、会话与安全附件处理。
3. `mail-brief-incremental`：日报水位线、去重、证据卡与输出。
4. `mail-imap-flag`：逐项确认后的标准星标。
5. `mail-compose-reply`：仅在对话中起草邮件。
6. `mail-smtp-send`：一封、已确认、保留会话头的 SMTP 发送。
7. `humanizer`：在不改变业务事实和承诺的前提下，对邮件草稿进行自然化润色。

旧名称 `business-email-managerment` 只保留兼容入口。新文档、代码和配置必须使用正确名称 `business-email-management`。

## 8. 数据、权限与安全

- 一台电脑只配置一位操作者的一项个人邮箱身份。
- 密码仅保存在操作系统凭据库；不得放入 `.env`、配置文件、日志、Git 或对话。
- 项目中的个人配置、状态数据库、临时附件和诊断日志必须被 `.gitignore` 排除。
- 状态数据只保留邮箱身份、处理时间、水位线、哈希和最少的语义卡片；最长 90 天。
- `READ` 和 `ANALYZE` 可自动执行；`DRAFT` 仅输出对话草稿；`WRITE`（星标）与 `SEND` 必须当前对话逐项确认。
- 不允许 `DELETE`、文件夹管理、自动发送或批量发送。

## 9. 文档与故障排查

V1 必须同时提供中文与英文版本：

- 安装指南：GitHub、ZIP、Windows、macOS、项目创建、模型选择、初始化验收。
- 使用指南：查邮件、读邮件、写草稿、发送、星标、日报。
- 升级指南：保留个人配置与状态，避免覆盖凭据。
- 故障排查：安全密码、IMAP/SMTP、管理员策略、网络、模型选择、附件限制、发送不确定状态。

故障信息必须使用通俗语言。技术日志仅在操作者需要进一步诊断时单独提供，且必须脱敏。

## 10. 测试与 READY 标准

### 必须通过

- Windows 与 macOS 路径解析和安装流程测试。
- 不同安装目录的可移植性测试；禁止任何个人电脑绝对路径。
- 凭据不出现在配置、日志、测试输出或 Git 追踪文件的检查。
- 只读 IMAP 查询不改变邮件已读状态的测试。
- 查询范围、UID 身份、会话标头和增量去重测试。
- 安全附件允许/阻止清单及临时文件清理测试。
- 未确认星标和未确认发送必须失败且不产生外部副作用。
- To/Cc/Bcc、回复和回复全部的最终确认及会话头测试。
- `humanizer` 不得更改确认过的事实、金额、日期、收件人、承诺或语言选择的回归测试。
- `humanizer` 对中英文商务草稿的自然度、完整性和禁用表达检查。
- 发送结果不确定时无自动重试测试。
- 中英文文档链接和安装步骤验证。
- 每日日报默认关闭、开启后不自动扩大权限的测试。

### READY 定义

只有在以下证据齐全时，V1 才可标为 `READY`：

1. 所有必需的自动化测试执行通过；
2. 真实阿里企业邮箱完成一次只读能力检查；
3. 操作者在新 Codex 项目内完成“Luna 查询 + Terra 草稿”验收；
4. 无密码泄露、无硬编码个人路径、无未确认发送或星标；
5. 中英文文档完整且可用。

未执行真实发送不妨碍 READY；真实发送只可由操作者针对一封具体邮件另行确认。
