# 升级指南

## 旧 Skill 名称

`business-email-managerment` 是历史拼写错误。它会保留为兼容入口，已有引用可以继续工作；所有新指令请使用 `business-email-management`。

## 安全升级

1. 保留原工作区；不要把密码或 `.email-steward` 数据复制到新的 ZIP。
2. 只替换新版发布包中的公开 Agent 文件；保留系统凭据库中的邮箱密码条目。
3. 在工作区重新打开 Codex 项目。读取使用 Luna、低推理；撰写使用 Terra、低推理加 `humanizer`。
4. 完成一次只读邮件查询和一次不发送的草稿；不要发送测试邮件。

若只读 IMAP 检查失败，请修正连接问题后重新运行安装器。它只会移除未完成的新安装，绝不会覆盖已有的非空工作区。
