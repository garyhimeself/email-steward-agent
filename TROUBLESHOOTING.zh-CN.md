# 故障排查

## 密码或登录失败

请使用阿里企业邮箱的**第三方客户端安全密码**，而不是网页登录密码。路径是：`设置 → 账户与安全 → 账户安全 → 三方客户端登录安全管理 → 生成新密码`。密码只显示一次，请在关闭页面前复制；之后只在本地隐藏窗口输入。

服务器拒绝登录不等于安全密码错误。请检查该账号是否允许第三方客户端登录、个人邮箱地址是否正确、管理员策略是否限制 IMAP，以及一次性密码是否完整复制。超时、TLS 或网络错误属于连接问题，不是密码诊断。不要把密码、原始服务器日志或包含这些内容的截图发到聊天。

## 只读 IMAP 检查失败

检查网络和邮箱地址后重新运行安装器。检查失败后，安装器会移除未完成工作区和已保存邮箱密码，因此重新运行安装器即可从干净状态重试。阿里企业邮箱默认服务器是 IMAP SSL `imap.qiye.aliyun.com:993` 和 SMTP SSL `smtp.qiye.aliyun.com:465`。连接失败不代表邮件被改动。如果提示“登录后只读邮箱检查失败”，请咨询邮箱管理员检查邮箱可用性，不要盲目更换密码。不要改用浏览器自动化绕过限制。

## 选错模型

查找、读取、分类和日报使用 **Luna、低推理**；撰写和最终措辞使用 **Terra、低推理** 加 `humanizer`。不要使用 Sol、高推理或更高推理。业务信息不明确时请向人确认。

## 附件打不开

Agent 只会临时处理安全的业务文件；可执行文件、脚本、压缩包、带宏文件、可疑类型或过大文件都会被拒绝。请让发件人提供安全的 PDF、普通 Office 文件、CSV、TXT 或图片，不要关闭这个限制。

## 发送显示“不确定”或有收件人被拒绝

请停止。不要再次点击发送，也不要要求自动重试：服务器可能已经投递给部分或全部收件人。请在已发送邮件中核查，或联系收件人/IT，再人工决定是否需要另发邮件。项目不提供自动发送。

## 密送、回复全部或星标无法执行

这些操作都需要当前对话中的具体确认。重新核对展示的 To、Cc、密送、来源邮件和正文后，只确认你要执行的那一项。密送会在确认时向你展示，但不会写入收件人收到的邮件头。星标还需要准确文件夹、UID、主题和理由。
