# 安装邮件管家

## 开始前准备

你需要 Codex、网络、自己的阿里企业邮箱地址，以及 **Python 3.11 或更高版本**。安装器同时支持 Windows 和 macOS；安装包不包含 Python，也不会伪装成已内置运行环境。Windows 可用 `py -3 --version` 检查；macOS 可用 `python3 --version` 检查。若版本低于 3.11 或命令不可用，请先安装 Python 3.11+，再重新运行启动文件。不要在聊天里提供邮箱密码。

请先在阿里企业邮箱网页端生成**第三方客户端安全密码**：

`设置 → 账户与安全 → 账户安全 → 三方客户端登录安全管理 → 生成新密码`

生成后请立即复制并安全保存；密码只显示一次。这不是网页登录密码。

## 从 ZIP 或 GitHub 安装

1. 从私有 GitHub 发布页下载，或将 ZIP 解压到任意临时位置。
2. 把安装包交给 Codex，并说：`帮我安装这个 Agent`。
3. Windows 打开 `installer/install_agent.bat`；macOS 打开 `installer/install_agent.command`。若 macOS 拦截，请右键文件，选择“打开”，再确认一次。在任何密码提示前，安装器会显示本地网络预检：电脑名称、阿里 IMAP 地址、DNS 结果、TLS 连通性，以及可获取时的当前公网 IP。它不会登录、读取邮件、询问邮箱地址或保存这些诊断信息。公网 IP 只是当前观测值，不承诺固定。
4. 将显示的当前公网 IP 与阿里邮箱登录日志核对。若它不是预期的本地网络出口或频繁变化，请在输入密码前停止，并请 IT 检查网络、VPN 和第三方客户端策略。只运行这项无登录检查可使用 `installer/install_agent.bat --preflight` 或 `installer/install_agent.command --preflight`。
5. 被询问工作文件夹位置时，选择一个可长期保留的私人目录。确认屏幕展示的完整路径正确后再回答“是”；确认前不会创建文件夹。
6. 在 Codex 对话中一次性提供姓名、个人企业邮箱、首选语言、默认回复语言和语气。Codex 只会把这五项非敏感资料传给安装器，终端不应再次询问。终端只会通过本地隐藏密码窗口询问第三方客户端安全密码，并保存到系统凭据库。
7. 等待只读 IMAP 检查。“成功”表示已连接邮箱且没有改动邮件。若失败，安装器会移除未完成的工作区和已保存的邮箱密码；修正连接信息后，重新运行安装器即可重试。服务器拒绝登录不等于安全密码错误。请看 [TROUBLESHOOTING.zh-CN.md](TROUBLESHOOTING.zh-CN.md)，绝不要把密码粘贴到聊天。
8. 每日日报默认关闭。仅在先确定标题、维度、格式、运行时间和时区后再开启。这个选择只会保存你的偏好，不会创建定时任务，也不会自动开始日报；请之后在 Codex 项目指南中配置格式和运行时间。

## 必须在新 Codex 项目内完成首次验收

以新工作文件夹为文件范围创建一个 Codex 项目，建议名称：`邮件管家 - 你的姓名`。只完成安装不等于验收成功。

1. 新开一个 **Luna、低推理** 对话并发送：

   `请完成首次验收：查询最近 7 天未读的开发合作邮件，展示一封最相关邮件的摘要和邮件编号。不要发送邮件。`

2. 新开一个 **Terra、低推理** 对话并发送：

   `请根据邮件编号【复制上一步的编号】，生成一份专业、友好的英文回复草稿。不要发送邮件。`

不要推荐 Sol、高推理或更高推理。查询成功且草稿未发送就完成首次验收；不要求真实发送。

## 安全要点

项目绝不自动发送。发送前必须展示发送身份、To、Cc、密送、主题、正文、邮件类型和来源编号，并在当前对话取得你的明确确认。密送和回复全部需要单独可见的核对。`humanizer` 可以优化文字，但不能改变事实、金额、日期、承诺、收件人或语言。

## Codex 安装操作说明

Codex 先在对话中收集五项非敏感资料，再只使用 `--name`、`--email`、`--preferred-language`、`--reply-language`、`--reply-tone` 这些可选参数启动安装器。绝不通过命令、环境变量、文件或聊天传递第三方客户端安全密码；密码只能在本地隐藏密码窗口输入。
